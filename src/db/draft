#run the scheme.sql in the db file to init the database
	sqlite3 userdata.db < scheme.sql

#QUERYS 
		SETUSER -> INSERT INTO users{<name>,<email>, ...};

		GET STUFF -> SELECT * FROM user (get all the tables)
					SELECT * FROM user WHERE <var> = x (get all data for specific user<var>)


