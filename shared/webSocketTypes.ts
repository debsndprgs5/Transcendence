// import { WebSocket } from 'ws';
// import { playerInterface } from './gameTypes';

// export type ServerWebSocket = WebSocket;
// export type ClientWebSocket = WebSocket;

// export interface TypedWebSocket {
//     socket: ServerWebSocket | ClientWebSocket;
//     send: (type: string, data: any) => void;
//     on: (event: string, handler: (data: any) => void) => void;
// }